#include <bits/stdc++.h>

#define FASTER                        \
    std::ios::sync_with_stdio(false); \
    std::cout.tie(NULL);              \
    std::cin.tie(NULL);

using namespace std;

int main() {
    FASTER;
    
    return 0;
}
