#include <bits/stdc++.h>

#define FASTER                        \
    std::ios::sync_with_stdio(false); \
    std::cin.tie(NULL);

using namespace std;

int main() {
    FASTER;
    
    return 0;
}
